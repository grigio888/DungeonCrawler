# Agent instructions

Start here: **[agents/README.md](agents/README.md)**

This project uses Cursor rules (`.cursor/rules/`) and skills (`.cursor/skills/`) wired to the `agents/` folder. Follow those guidelines on every iteration over `src/lib/` and `tests/`.
