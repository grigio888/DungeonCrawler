/** Scenario / map background prompts — loaders to be implemented. */

export const PROMPTS_DIR = 'scenarios/assets/prompts';

/**
 * @param {string} scenarioId e.g. `forest_clearing`
 */
export function resolvePromptPath(scenarioId) {
	return scenarioId;
}
